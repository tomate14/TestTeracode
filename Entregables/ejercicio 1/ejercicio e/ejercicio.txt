SELECT name, lastName
FROM PERSON P
WHERE EXISTS (
				SELECT 1
				FROM EMPLOYEE E INNER JOIN JANITOR J ON E.id = J.id
				WHERE J.workingArea = "Hallway"	
				  AND P.id = J.id
			)

Primero hace el from, luego evalua si existe alguno en las personas y luego recupera el nombre y apellido